model Client {
  id                  Int     @id @default(autoincrement())
  address             String
  disclaimer_accepted Boolean

  // Foreign Key Relationships 
  account Account?
  jobs    Job[]
}
