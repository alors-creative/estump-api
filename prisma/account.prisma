model Account {
  id           Int    @id @default(autoincrement())
  first_name   String
  last_name    String
  phone_number String
  email        String @unique

  // Foreign Key Relationships 
  // type_of_account_id
}
